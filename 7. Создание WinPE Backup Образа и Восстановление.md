# Полное руководство: Создание WinPE Backup Образа и Восстановление

## ЧАСТЬ 1: СОЗДАНИЕ WINPE ЗАГРУЗОЧНОГО ДИСКА

### Шаг 1: Подготовка рабочих папок
```powershell
# Создаем основную рабочую папку для WinPE
mkdir C:\WinPE_Work -Force
```
**Комментарий:** Создаем папку где будут собираться все файлы WinPE

### Шаг 2: Копирование базовых файлов WinPE
```powershell
# Копируем все файлы из Media папки ADK
$SourceMedia = "C:\Program Files (x86)\Windows Kits\10\Assessment and Deployment Kit\Windows Preinstallation Environment\amd64\Media"
$DestMedia = "C:\WinPE_Work\media"
Copy-Item "$SourceMedia\*" $DestMedia -Recurse -Force
```
**Комментарий:** Копируем загрузочные файлы, BCD, шрифты, мультиязычные файлы

### Шаг 3: Создание папки sources
```powershell
# Создаем папку sources для boot.wim
mkdir "C:\WinPE_Work\media\sources" -Force
```
**Комментарий:** Папка sources обязательна для загрузочного WIM-файла

### Шаг 4: Копирование WinPE образа
```powershell
# Копируем основной WinPE образ как boot.wim
Copy-Item "C:\Program Files (x86)\Windows Kits\10\Assessment and Deployment Kit\Windows Preinstallation Environment\amd64\en-us\winpe.wim" "C:\WinPE_Work\media\sources\boot.wim" -Force
```
**Комментарий:** WinPE.wim становится boot.wim - это минимальная Windows для загрузки

### Шаг 5: Копирование загрузочных секторов
```powershell
# Копируем загрузочные сектора для BIOS и UEFI
Copy-Item "C:\Program Files (x86)\Windows Kits\10\Assessment and Deployment Kit\Deployment Tools\amd64\Oscdimg\etfsboot.com" "C:\WinPE_Work\media\boot\etfsboot.com" -Force
Copy-Item "C:\Program Files (x86)\Windows Kits\10\Assessment and Deployment Kit\Deployment Tools\amd64\Oscdimg\efisys.bin" "C:\WinPE_Work\media\efi\microsoft\boot\efisys.bin" -Force
```
**Комментарий:** 
- `etfsboot.com` - загрузчик для BIOS систем
- `efisys.bin` - загрузчик для UEFI систем

### Шаг 6: Проверка файлов
```powershell
# Проверяем что все ключевые файлы на месте
Write-Host "Проверка файлов WinPE:" -ForegroundColor Yellow
Test-Path "C:\WinPE_Work\media\boot\etfsboot.com"
Test-Path "C:\WinPE_Work\media\efi\microsoft\boot\efisys.bin" 
Test-Path "C:\WinPE_Work\media\sources\boot.wim"
Test-Path "C:\WinPE_Work\media\boot\boot.sdi"

# Показываем размер boot.wim
$BootWim = Get-ChildItem "C:\WinPE_Work\media\sources\boot.wim"
Write-Host "Размер boot.wim: $([math]::Round($BootWim.Length/1MB, 2)) MB" -ForegroundColor Green
```
**Комментарий:** Проверяем что все необходимые файлы скопированы корректно

### Шаг 7: Создание ISO образа
```powershell
# Переходим в папку с oscdimg
cd "C:\Program Files (x86)\Windows Kits\10\Assessment and Deployment Kit\Deployment Tools\amd64\Oscdimg"

# Создаем загрузочный ISO
.\oscdimg.exe -m -o -u2 -udfver102 -l"Win11_Backup_Recovery" -bootdata:2#p0,e,bC:\WinPE_Work\media\boot\etfsboot.com#pEF,e,bC:\WinPE_Work\media\efi\microsoft\boot\efisys.bin C:\WinPE_Work\media "C:\Win11_Backup_Recovery.iso"
```
**Комментарий:**
- `-m` - игнорировать ограничение размера
- `-o` - оптимизация, объединение дубликатов
- `-u2` - UDF файловая система версии 2
- `-l` - метка диска
- `-bootdata` - параметры загрузки для BIOS и UEFI

### Шаг 8: Проверка созданного ISO
```powershell
# Проверяем результат
if (Test-Path "C:\Win11_Backup_Recovery.iso") {
    $iso = Get-ChildItem "C:\Win11_Backup_Recovery.iso"
    Write-Host "✅ WinPE ISO успешно создан!" -ForegroundColor Green
    Write-Host "📁 Имя: $($iso.Name)" -ForegroundColor Yellow
    Write-Host "📊 Размер: $([math]::Round($iso.Length/1MB, 2)) MB" -ForegroundColor Cyan
    Write-Host "📍 Путь: $($iso.FullName)" -ForegroundColor White
    
    # Копируем на диск S: для надежности
    Copy-Item "C:\Win11_Backup_Recovery.iso" "S:\" -Force
    Write-Host "📋 Копия создана на S:\" -ForegroundColor Green
} else {
    Write-Host "❌ Ошибка создания ISO" -ForegroundColor Red
}
```

---

## ЧАСТЬ 2: СОЗДАНИЕ BACKUP ОБРАЗА СИСТЕМЫ

### Шаг 9: Создание загрузочной флешки
**Действия:**
1. Скачайте и установите **Rufus** (rufus.ie)
2. Подключите флешку (минимум 8 GB)
3. Запустите Rufus:
   - **Устройство:** Ваша флешка
   - **Метод загрузки:** ВЫБРАТЬ → `C:\Win11_Backup_Recovery.iso`
   - **Схема разделов:** GPT (для UEFI)
   - **Файловая система:** NTFS
   - **Метка тома:** `Win11_Backup`
   - **Нажмите:** СТАРТ

### Шаг 10: Загрузка с WinPE флешки
**Действия:**
1. Перезагрузите компьютер
2. При запуске нажмите **F12** (или F2, Del, Esc - зависит от материнской платы)
3. В Boot Menu выберите вашу флешку
4. Система загрузится в WinPE (черный экран с командной строкой)

### Шаг 11: Создание образа системы в WinPE
```cmd
# В среде WinPE выполняем:
diskpart
list volume
exit

# Создаем backup образа
dism /Capture-Image /ImageFile:S:\Windows11_Backup.wim /CaptureDir:C:\ /Name:"Windows11_Complete_Backup" /Description:"Full system backup created $(Get-Date)" /Compress:max /CheckIntegrity /Verify
```
**Комментарий параметров:**
- `/ImageFile` - куда сохранять образ
- `/CaptureDir` - что архивируем (диск C:)
- `/Name` - название образа
- `/Compress:max` - максимальное сжатие
- `/CheckIntegrity` - проверка целостности
- `/Verify` - верификация файлов

### Шаг 12: Проверка созданного образа
```cmd
# Проверяем созданный WIM-файл
dism /Get-WimInfo /WimFile:S:\Windows11_Backup.wim
```
**Ожидаемый результат:** Информация об образе с размером 15-25 GB

---

## ЧАСТЬ 3: ВОССТАНОВЛЕНИЕ СИСТЕМЫ ИЗ ОБРАЗА

### Способ A: Восстановление через WinPE

#### Шаг 1: Загрузка с WinPE флешки
(Повторить Шаг 10)

#### Шаг 2: Очистка целевого диска
```cmd
# Запускаем diskpart
diskpart

# Показываем все диски
list disk

# Выбираем диск для восстановления (ОСТОРОЖНО!)
select disk 0

# Очищаем диск
clean

# Создаем разделы для UEFI
create partition efi size=100
format quick fs=fat32 label="System"
assign letter="S"

create partition msr size=16

create partition primary
format quick fs=ntfs label="Windows"
assign letter="C"

exit
```

#### Шаг 3: Восстановление образа
```cmd
# Восстанавливаем систему из backup
dism /Apply-Image /ImageFile:S:\Windows11_Backup.wim /Index:1 /ApplyDir:C:\

# Создаем загрузочную запись
bcdboot C:\Windows /s S: /f ALL
```

#### Шаг 4: Проверка восстановления
```cmd
# Проверяем что файлы на месте
dir C:\Windows\System32

# Перезагружаемся
wpeutil reboot
```

### Способ B: Восстановление поверх существующей системы

#### Шаг 1: Загрузка с WinPE
(Аналогично Способу A)

#### Шаг 2: Очистка только системных файлов
```cmd
# Форматируем только системный раздел
diskpart
select volume C
format quick fs=ntfs label="Windows"
exit
```

#### Шаг 3: Восстановление
```cmd
# Применяем образ
dism /Apply-Image /ImageFile:S:\Windows11_Backup.wim /Index:1 /ApplyDir:C:\

# Восстанавливаем загрузчик
bcdboot C:\Windows
```

---

## ЧАСТЬ 4: ПРОВЕРКА И ДОПОЛНИТЕЛЬНЫЕ ВОЗМОЖНОСТИ

### Проверка целостности образа
```cmd
# Проверить образ без восстановления
dism /Get-WimInfo /WimFile:S:\Windows11_Backup.wim

# Проверить целостность
dism /Check-Image /ImageFile:S:\Windows11_Backup.wim
```

### Разбиение большого образа на части
```cmd
# Если образ больше 4GB (для FAT32)
dism /Split-Image /ImageFile:S:\Windows11_Backup.wim /SWMFile:S:\Windows11_Backup.swm /FileSize:4000
```

### Добавление драйверов в образ
```cmd
# Монтируем образ для модификации
dism /Mount-Wim /WimFile:S:\Windows11_Backup.wim /Index:1 /MountDir:C:\Mount

# Добавляем драйверы
dism /Image:C:\Mount /Add-Driver /Driver:D:\Drivers /Recurse

# Размонтируем с сохранением
dism /Unmount-Wim /MountDir:C:\Mount /Commit
```

---

## ⚠️ ВАЖНЫЕ ПРЕДУПРЕЖДЕНИЯ:

1. **Резервное копирование:** Всегда храните backup на отдельном диске
2. **Тестирование:** Проверьте восстановление на виртуальной машине перед реальным использованием
3. **Активация:** После восстановления может потребоваться реактивация Windows
4. **Драйверы:** На другом hardware могут потребоваться другие драйверы
5. **Размер:** Убедитесь что на целевом диске достаточно места

## 📁 ИТОГОВЫЕ ФАЙЛЫ:
- `Win11_Backup_Recovery.iso` - загрузочный WinPE
- `Windows11_Backup.wim` - образ вашей системы

**Теперь у вас есть полная система резервного копирования и восстановления!** 🎉
