## Установка и настройка ALT Linux 11 Starterkit в WSL 2  
[Оригинальная инструкция](https://быстрыеотчеты.рф/blogs/wsl-fastreport-fastcube)

---

### 🔧 Установка ALT Linux в WSL 2

**WSL 2 позволяет использовать любой дистрибутив Linux, импортируя его из tar-файла.**

#### 1. Скачивание образа
- **Для стабильной версии (p11):**  
  ```bash
  .\curl.exe -o C:\alt-p11-rootfs-systemd-x86_64.tar.xz https://ftp.altlinux.org/pub/distributions/ALTLinux/p11/images/cloud/x86_64/alt-p11-rootfs-systemd-x86_64.tar.xz
  ```

- **Для Sisyphus (тестовый репозиторий):**  
  ```bash
  .\curl.exe -o C:\alt-sisyphus-rootfs-systemd-x86_64.tar.xz https://ftp.altlinux.org/pub/distributions/ALTLinux/images/Sisyphus/cloud/x86_64/alt-sisyphus-rootfs-systemd-x86_64.tar.xz
  ```

#### 2. Импорт в WSL 2
Выполните в **командной строке Windows**:

```bash
wsl --import ALTLinux-11 C:\ALTLinux-11 C:\alt-p11-rootfs-systemd-x86_64.tar.xz
wsl --import ALTLinux-Sisyphus C:\ALTLinux-Sisyphus C:\alt-sisyphus-rootfs-systemd-x86_64.tar.xz
```

> 📁 После импорта образ будет расположен по пути:  
> `C:\ALTLinux-11\ext4.vhdx`

---

### ✅ Запуск и проверка

#### Просмотр установленных дистрибутивов:
```bash
wsl -l -v
```

#### Запуск ALT Linux:
```bash
wsl -d ALTLinux-11
wsl -d ALTLinux-Sisyphus
```

---

### ⚙️ Настройка systemd

После запуска откроется терминал.  
Разрешите использование systemd, создав конфигурационный файл:

```bash
echo -e "[boot]\nsystemd=true\n" > /etc/wsl.conf
```

#### Перезапуск дистрибутива:
```bash
exit
wsl -t ALTLinux-11
wsl -t ALTLinux-Sisyphus

wsl -d ALTLinux-11
wsl -d ALTLinux-Sisyphus
```

---

### 🌍 Русификация

Установите русскую локализацию:

```bash
echo -e "LANG=ru_RU.UTF-8" > /etc/default/locale
```

---

### 📦 Обновление системы

Обновите пакеты:

```bash
apt-get update && apt-get dist-upgrade
```

> При появлении запроса на обновление — введите `Y` и нажмите **Enter**.

---

### 🖋 Установка шрифтов и файлового менеджера

#### Установка шрифтов Microsoft:
```bash
apt-get install fonts-ttf-ms
```

#### Установка и запуск Nautilus:
```bash
sudo apt install nautilus -y
nautilus
```

---

