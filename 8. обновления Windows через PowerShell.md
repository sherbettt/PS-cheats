### 📜 Последовательность обновления Windows через PowerShell
Вот полный алгоритм действий, который мы выполнили:

**1. Установка модуля PSWindowsUpdate** (требуется один раз)
```powershell
# В PowerShell (не админ) разрешить установку из репозитория PSGallery
Install-Module PSWindowsUpdate -Force -Confirm:$false
```
*(Именно на этом шаге вы столкнулись с предупреждениями о ненадёжном репозитории и поставщике NuGet — нужно было ответить `Y`)*

**2. Разрешение выполнения скриптов в системе** (ваш главный блокер)
```powershell
# Запустите PowerShell ОТ ИМЕНИ АДМИНИСТРАТОРА
# 2.1. Установить политику выполнения RemoteSigned для всей системы
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned
# Ответить Y на запрос подтверждения
```
*(Вы сначала попытались изменить политику без прав администратора и получили ошибку о переопределении. Ключевое — запуск от администратора)*

**3. Импорт модуля и проверка обновлений** (в обычной консоли)
```powershell
# 3.1. Импортировать модуль (теперь будет работать)
Import-Module PSWindowsUpdate

# 3.2. Проверить доступные обновления
Get-WindowsUpdate
```
*(Вот что вы увидели в результате:)*
```
DESKTOP-3... -------    KB5072033   89GB 2025-12 Обновление для системы безопасности (KB5072033)
```

**4. Установка обновлений** (требует прав администратора)
```powershell
# Запустите PowerShell ОТ ИМЕНИ АДМИНИСТРАТОРА снова
# 4.1. Импортировать модуль в сеанс администратора
Import-Module PSWindowsUpdate

# 4.2. Установить ВСЕ найденные обновления с автоматической перезагрузкой
Install-WindowsUpdate -AcceptAll -AutoReboot
```
*Параметры:*
- **`-AcceptAll`** — согласиться на установку всех обновлений без подтверждения
- **`-AutoReboot`** — автоматически перезагрузить систему, если требуется

### ⚠️ **Критически важные моменты из вашего опыта**
1.  **Права администратора** — нужны для:
    - Изменения политики выполнения (`Set-ExecutionPolicy`)
    - Непосредственно установки обновлений (`Install-WindowsUpdate`)
    - Активации Windows (`slmgr.vbs /ato`)

2.  **Политика выполнения PowerShell** — по умолчанию была `Restricted`, что блокировало любые скрипты. Мы изменили её на `RemoteSigned`, чтобы разрешить выполнение локальных модулей (включая `PSWindowsUpdate`).

3.  **Размер обновления** — 89 ГБ указывает на то, что система давно не обновлялась. Установка может занять **несколько часов**.

### 🔄 **Алгоритм действий в будущем**
Теперь, когда всё настроено, для следующих обновлений достаточно:
1.  Открыть PowerShell (не обязательно админ)
2.  Выполнить:
    ```powershell
    Import-Module PSWindowsUpdate
    Get-WindowsUpdate
    ```
3.  Если есть обновления, запустить PowerShell **от администратора** и выполнить:
    ```powershell
    Import-Module PSWindowsUpdate
    Install-WindowsUpdate -AcceptAll -AutoReboot
    ```

### 📌 **Сводка по основным командам**
| Действие | Команда (в PowerShell) | Требует админа? |
|----------|------------------------|-----------------|
| **Установить модуль** | `Install-Module PSWindowsUpdate -Force` | Нет |
| **Изменить политику** | `Set-ExecutionPolicy RemoteSigned` | **Да** |
| **Проверить обновления** | `Get-WindowsUpdate` | Нет |
| **Установить всё** | `Install-WindowsUpdate -AcceptAll -AutoReboot` | **Да** |
| **Активировать Windows** | `cscript slmgr.vbs /ato` | **Да** |

### 💡 **Что делать, если снова возникнут ошибки**
Если при импорте модуля снова появится ошибка о политике выполнения:
1.  **Проверить текущую политику**: `Get-ExecutionPolicy -List`
2.  **Если `LocalMachine` не `RemoteSigned`**, снова установить её от администратора.

Если установка обновлений не начинается:
1.  Убедитесь, что PowerShell запущен **от имени администратора**.
2.  Попробуйте альтернативную команду:
    ```powershell
    Get-WindowsUpdate -Install -AcceptAll -AutoReboot
    ```

